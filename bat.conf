# Ankh Dark companion configuration for bat.
# Copy or merge into ~/.config/bat/config. `ansi` follows the Omarchy terminal palette.
--theme="ansi"
--style="numbers,changes,header,grid"
--italic-text=always
